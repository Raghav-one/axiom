# Axiom

A visual, source-backed reference library for artificial intelligence, from computing and mathematics through systems, models, hardware, evaluation, and practice.

## Local development

```bash
npm install
npm run build
npm run dev
```

The production site is deployed through GitHub Pages on every push to `main`.
