{
  "name": "axiom-ai-field-library",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "start": "npm run content && vite --host 127.0.0.1",
    "content": "node scripts/build-content.mjs && node scripts/prepare-static.mjs",
    "build": "npm run content && vite build",
    "serve": "vite preview --host 127.0.0.1",
    "test": "node tests/app.mjs"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "parse5": "^7.3.0"
  },
  "devDependencies": {
    "playwright": "^1.51.1",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=20"
  }
}
