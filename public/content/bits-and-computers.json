{"id":"bits-and-computers","title":"Bits, bytes & how computers work","group":"computing","summary":"Begin with the physical and logical foundations: binary numbers, instructions, memory, storage, and the CPU.","prerequisites":["ai-map"],"level":"Foundation","origin":"New foundation","sources":[["OSTEP: Operating Systems","https://pages.cs.wisc.edu/~remzi/OSTEP/"]],"html":"<p>Begin with the physical and logical foundations: binary numbers, instructions, memory, storage, and the CPU.</p><h2 id=\"part-1\">How the pieces connect</h2><div class=\"diagram authored-diagram\"><svg viewBox=\"0 0 720 290\" xmlns=\"http://www.w3.org/2000/svg\" role=\"img\" aria-label=\"Bits, bytes &amp; how computers work workflow\"><defs><marker id=\"arrow-32\" markerWidth=\"8\" markerHeight=\"8\" refX=\"7\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L7,3 L0,6 Z\" fill=\"#7690b7\"/></marker></defs><text x=\"28\" y=\"39\" class=\"diagram-overline\">MENTAL MODEL</text><text x=\"28\" y=\"64\" class=\"diagram-heading\">Bits, bytes &amp; how computers work — from question to evidence</text><g><rect x=\"28\" y=\"87\" width=\"142\" height=\"132\" rx=\"8\"/><text x=\"42\" y=\"116\" class=\"step\">01</text><text x=\"42\" y=\"141\" class=\"diagram-title\"><tspan x=\"42\" dy=\"0\">Encode</tspan></text><text x=\"42\" y=\"177\" class=\"diagram-detail\"><tspan x=\"42\" dy=\"0\">Represent a value as</tspan><tspan x=\"42\" dy=\"12\">bits and a datatype.</tspan></text><path class=\"connector\" d=\"M171 153H197\" marker-end=\"url(#arrow-32)\"/></g><g><rect x=\"199\" y=\"87\" width=\"142\" height=\"132\" rx=\"8\"/><text x=\"213\" y=\"116\" class=\"step\">02</text><text x=\"213\" y=\"141\" class=\"diagram-title\"><tspan x=\"213\" dy=\"0\">Move</tspan></text><text x=\"213\" y=\"177\" class=\"diagram-detail\"><tspan x=\"213\" dy=\"0\">Fetch it through</tspan><tspan x=\"213\" dy=\"12\">storage, RAM, cache,</tspan><tspan x=\"213\" dy=\"12\">and registers.</tspan></text><path class=\"connector\" d=\"M342 153H368\" marker-end=\"url(#arrow-32)\"/></g><g><rect x=\"370\" y=\"87\" width=\"142\" height=\"132\" rx=\"8\"/><text x=\"384\" y=\"116\" class=\"step\">03</text><text x=\"384\" y=\"141\" class=\"diagram-title\"><tspan x=\"384\" dy=\"0\">Execute</tspan></text><text x=\"384\" y=\"177\" class=\"diagram-detail\"><tspan x=\"384\" dy=\"0\">Apply instructions</tspan><tspan x=\"384\" dy=\"12\">using CPU or</tspan><tspan x=\"384\" dy=\"12\">accelerator units.</tspan></text><path class=\"connector\" d=\"M513 153H539\" marker-end=\"url(#arrow-32)\"/></g><g><rect x=\"541\" y=\"87\" width=\"142\" height=\"132\" rx=\"8\"/><text x=\"555\" y=\"116\" class=\"step\">04</text><text x=\"555\" y=\"141\" class=\"diagram-title\"><tspan x=\"555\" dy=\"0\">Persist</tspan></text><text x=\"555\" y=\"177\" class=\"diagram-detail\"><tspan x=\"555\" dy=\"0\">Write results with an</tspan><tspan x=\"555\" dy=\"12\">explicit format and</tspan><tspan x=\"555\" dy=\"12\">lifetime.</tspan></text></g></svg><div class=\"diagram-caption\">Encode → Move → Execute → Persist. Each transition is a place where an assumption can be inspected before the next layer of the system compounds it.</div></div><h2 id=\"part-2\">From voltage to information</h2><p>Digital circuits interpret ranges of electrical states as 0 and 1. A bit stores one binary choice. Eight bits form a byte and encode 256 distinct patterns. Meaning comes from an agreed representation: the same bits can be interpreted as an integer, a character, or part of a floating-point number.</p><h2 id=\"part-3\">A program is data plus instructions</h2><p>A processor fetches instructions, decodes them, operates on registers, and reads or writes memory. Modern CPUs overlap work, predict branches, and cache recently used data. A thread is an instruction stream with its own execution state. Multiple cores can execute multiple streams; dependencies still limit what can happen simultaneously.</p><h2 id=\"part-4\">The storage hierarchy</h2><p>Registers are small and close to arithmetic units. Caches retain likely-to-be-reused memory. RAM holds a running program’s working set. SSDs persist files but are much slower for fine-grained access. Moving a model from disk to RAM to GPU memory is real work, even if a framework hides the transfers.</p><h2 id=\"part-5\">Units that prevent expensive mistakes</h2><p>A decimal GB is 10^9 bytes; a GiB is 2^30 bytes. Bandwidth is data per second; latency is time until a result arrives. Frequency counts clock cycles per second, not completed useful operations. Model parameter count measures stored learned values, not token capacity or reasoning ability.</p><h2 id=\"part-6\">Worked example</h2><p>An 8-billion-parameter model with one 16-bit value per parameter needs 8×10^9×2 = 16 GB, or about 14.90 GiB, for weights alone. Runtime buffers and a KV cache consume additional memory. A 16 GB device is therefore not guaranteed to fit it.</p><h2 id=\"part-7\">What goes wrong</h2><p>Confusing Gb with GB introduces an eightfold error. Assuming all installed memory is available ignores the operating system, other processes, and runtime allocations.</p><div class=\"exercise\"><span class=\"eyebrow\">Check your understanding</span><p>How many distinct patterns fit in 12 bits?</p><details><summary>Reveal the explanation</summary><p>2^12 = 4,096 patterns. This says nothing about their meaning until an encoding is chosen.</p></details></div><h2 id=\"part-8\">An operating model for bits, bytes & how computers work</h2><p>Every apparent software abstraction eventually becomes a memory layout and a sequence of instructions. A model checkpoint is bytes on storage; loading it establishes tensors in memory; inference schedules arithmetic; a response becomes network bytes. This path explains why loading, serialization, and copying can be more important than FLOPS for small workloads.</p><p><strong>The gotcha.</strong> A clean workflow diagram describes dependency order. It does not prove that any boundary is correct; each transition needs its own observable acceptance test.</p><h2 id=\"part-9\">Worked system: loading a model is a data-movement problem</h2><p>Consider a 7-billion-parameter model stored in a two-byte format. The weights alone occupy roughly 14 billion bytes before file metadata, alignment, runtime allocations, or a key-value cache. Opening the file does not make it runnable: storage reads move bytes into main memory; the framework interprets those bytes as tensors; a GPU runtime may then copy or map them into device memory; only then can arithmetic begin. The slowest stage is often not multiplication. A local machine with a fast GPU can still feel slow because the checkpoint is on a slow external drive, because memory pressure forces paging, or because the model is copied twice by an accidental dtype conversion.</p><p>That path also explains why an apparent “out of memory” error is a structural diagnosis. Ask which allocation is live at the failure: immutable weights, a growing KV cache, temporary activations, a duplicate host copy, or an allocator reservation. Lowering batch size may hide the symptom, but it does not identify the owner of the bytes. The useful habit is to make a ledger before changing hardware: count elements, choose a dtype, multiply by bytes per element, then name the location and lifetime of each allocation.</p><h2 id=\"part-10\">Notebook: Trace a byte budget</h2><p>For any performance or capacity claim, name the representation, bytes per element, working-set size, storage location, and transfer path. Those five facts prevent most unit mistakes.</p><pre><code>items = 1_000_000\nbytes_per_item = 4\nworking_set = items * bytes_per_item\nprint(f\"{working_set / 2**20:.1f} MiB\")\n# Now include every copy, index, and temporary buffer.</code></pre><div class=\"callout warning\"><strong>What to test next.</strong> 1. Encode: Represent a value as bits and a datatype. 2. Move: Fetch it through storage, RAM, cache, and registers. 3. Execute: Apply instructions using CPU or accelerator units. 4. Persist: Write results with an explicit format and lifetime.</div><p><strong>The gotcha.</strong> A small inspection script proves only the quantities it prints. Preserve units, shapes, versions, and the input fixture beside its output.</p><h2 id=\"part-11\">A rigorous way to practice</h2><ol><li><strong>Encode.</strong> Represent a value as bits and a datatype.</li><li><strong>Move.</strong> Fetch it through storage, RAM, cache, and registers.</li><li><strong>Execute.</strong> Apply instructions using CPU or accelerator units.</li><li><strong>Persist.</strong> Write results with an explicit format and lifetime.</li></ol><p>For any performance or capacity claim, name the representation, bytes per element, working-set size, storage location, and transfer path. Those five facts prevent most unit mistakes.</p><h2 id=\"part-12\">Verification questions for bits, bytes & how computers work</h2><div class=\"table-scroll\"><table><thead><tr><th>Boundary</th><th>Question</th><th>Evidence</th></tr></thead><tbody><tr><td>Encode</td><td>What would show that this boundary failed?</td><td>Represent a value as bits and a datatype.</td></tr><tr><td>Move</td><td>What would show that this boundary failed?</td><td>Fetch it through storage, RAM, cache, and registers.</td></tr><tr><td>Execute</td><td>What would show that this boundary failed?</td><td>Apply instructions using CPU or accelerator units.</td></tr><tr><td>Persist</td><td>What would show that this boundary failed?</td><td>Write results with an explicit format and lifetime.</td></tr></tbody></table></div><p><strong>The gotcha.</strong> Passing each component check separately does not prove the composed path preserves identity, units, ordering, or authority.</p><h2 id=\"part-13\">One bit pattern, several meanings</h2><p>A bit pattern has no intrinsic type. The consuming instruction decides whether it represents an unsigned integer, two's-complement integer, floating-point value, text, address, or flags.</p><ul><li><strong>Unsigned integers spend every pattern on magnitude.</strong> An n-bit value covers 0 through 2ⁿ−1 and usually wraps modulo 2ⁿ.</li><li><strong>IEEE 754 trades uniform spacing for dynamic range.</strong> Sign, exponent, and significand encode ordinary values plus NaN, infinities, signed zero, and subnormals.</li><li><strong>UTF-8 encodes Unicode code points as bytes.</strong> A visible grapheme may contain several code points, so byte length and character count differ.</li></ul><p><strong>The gotcha.</strong> Reinterpreting bytes changes meaning without converting value.</p><h2 id=\"part-14\">Memory is a hierarchy</h2><p>Registers, caches, RAM, and storage trade capacity against latency and bandwidth. Dense scans exploit locality; pointer chasing and random page access often do not.</p><ul><li><strong>Caches move lines.</strong> Reading one field fetches nearby bytes, and unrelated writes on one line can create false sharing.</li><li><strong>Virtual memory translates and protects addresses.</strong> Page tables and translation caches add another locality boundary.</li><li><strong>Capacity includes live intermediates.</strong> Parameters coexist with activations, gradients, optimizer state, workspaces, padding, and copies.</li></ul><p><strong>The gotcha.</strong> A parameter file can fit while the running model fails at its memory peak.</p><h2 id=\"part-15\">Instructions expose physical cost</h2><p>Compilers lower source into an instruction set. Pipelines overlap stages, out-of-order execution finds independent work, vector units operate on lanes, and atomic operations coordinate selected shared updates.</p><ul><li><strong>Dependencies set the parallel ceiling.</strong> More cores cannot accelerate a serial chain.</li><li><strong>Memory ordering belongs to correctness.</strong> An atomic counter does not protect a multi-step invariant.</li><li><strong>Formats preserve meaning across machines.</strong> Endianness, sizes, schema versions, units, and checksums need explicit contracts.</li></ul><p><strong>The gotcha.</strong> Successful parsing proves structure, not semantic compatibility.</p>","headings":[{"id":"part-1","title":"How the pieces connect"},{"id":"part-2","title":"From voltage to information"},{"id":"part-3","title":"A program is data plus instructions"},{"id":"part-4","title":"The storage hierarchy"},{"id":"part-5","title":"Units that prevent expensive mistakes"},{"id":"part-6","title":"Worked example"},{"id":"part-7","title":"What goes wrong"},{"id":"part-8","title":"An operating model for bits, bytes & how computers work"},{"id":"part-9","title":"Worked system: loading a model is a data-movement problem"},{"id":"part-10","title":"Notebook: Trace a byte budget"},{"id":"part-11","title":"A rigorous way to practice"},{"id":"part-12","title":"Verification questions for bits, bytes & how computers work"},{"id":"part-13","title":"One bit pattern, several meanings"},{"id":"part-14","title":"Memory is a hierarchy"},{"id":"part-15","title":"Instructions expose physical cost"}],"words":1257,"minutes":7,"number":"02"}