{"id":"calculus-and-gradients","title":"Calculus, gradients & the chain rule","group":"mathematics","summary":"Understand what a derivative measures and how a network assigns responsibility for error.","prerequisites":["linear-algebra"],"level":"Foundation","origin":"New foundation","sources":[["Mathematics for Machine Learning","https://mml-book.github.io/"],["PyTorch tutorials","https://docs.pytorch.org/tutorials/"]],"html":"<p>Understand what a derivative measures and how a network assigns responsibility for error.</p><h2 id=\"part-1\">How the pieces connect</h2><figure class=\"diagram authored-diagram\"><header class=\"diagram-header\"><span>MENTAL MODEL</span><h3>Calculus, gradients &amp; the chain rule</h3><p>From question to evidence</p></header><div class=\"diagram-flow\"><svg viewBox=\"0 0 730 148\" xmlns=\"http://www.w3.org/2000/svg\" role=\"img\" aria-label=\"Calculus, gradients &amp; the chain rule workflow\"><defs><marker id=\"arrow-calculus-gradients-the-chain-rule\" markerWidth=\"8\" markerHeight=\"8\" refX=\"7\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L7,3 L0,6 Z\" fill=\"#a43b19\"/></marker></defs><g class=\"diagram-node\"><rect x=\"20\" y=\"20\" width=\"155\" height=\"108\" rx=\"8\"/><text x=\"36\" y=\"49\" class=\"step\">01</text><text x=\"36\" y=\"78\" class=\"diagram-title\"><tspan x=\"36\" dy=\"0\">Forward pass</tspan></text><path class=\"connector\" d=\"M176 74H202\" marker-end=\"url(#arrow-calculus-gradients-the-chain-rule)\"/></g><g class=\"diagram-node\"><rect x=\"205\" y=\"20\" width=\"155\" height=\"108\" rx=\"8\"/><text x=\"221\" y=\"49\" class=\"step\">02</text><text x=\"221\" y=\"78\" class=\"diagram-title\"><tspan x=\"221\" dy=\"0\">Local derivatives</tspan></text><path class=\"connector\" d=\"M361 74H387\" marker-end=\"url(#arrow-calculus-gradients-the-chain-rule)\"/></g><g class=\"diagram-node\"><rect x=\"390\" y=\"20\" width=\"155\" height=\"108\" rx=\"8\"/><text x=\"406\" y=\"49\" class=\"step\">03</text><text x=\"406\" y=\"78\" class=\"diagram-title\"><tspan x=\"406\" dy=\"0\">Backward pass</tspan></text><path class=\"connector\" d=\"M546 74H572\" marker-end=\"url(#arrow-calculus-gradients-the-chain-rule)\"/></g><g class=\"diagram-node\"><rect x=\"575\" y=\"20\" width=\"155\" height=\"108\" rx=\"8\"/><text x=\"591\" y=\"49\" class=\"step\">04</text><text x=\"591\" y=\"78\" class=\"diagram-title\"><tspan x=\"591\" dy=\"0\">Update</tspan></text></g></svg></div><ol class=\"diagram-stage-list\"><li><span class=\"diagram-step-number\">01</span><div><strong>Forward pass</strong><p>Compute intermediate values and the loss.</p></div></li><li><span class=\"diagram-step-number\">02</span><div><strong>Local derivatives</strong><p>Record how each operation changes nearby values.</p></div></li><li><span class=\"diagram-step-number\">03</span><div><strong>Backward pass</strong><p>Apply the chain rule from loss to parameters.</p></div></li><li><span class=\"diagram-step-number\">04</span><div><strong>Update</strong><p>Use the gradients with a controlled optimizer step.</p></div></li></ol><figcaption class=\"diagram-caption\">Forward pass → Local derivatives → Backward pass → Update. Each transition is a place where an assumption can be inspected before the next layer of the system compounds it.</figcaption></figure><h2 id=\"part-2\">A derivative measures local change</h2><p>For a scalar function, the derivative describes how the output changes for a small change in input. It is a local approximation, not a prediction over an arbitrary distance. A gradient collects partial derivatives with respect to many parameters. Its negative points toward steepest local decrease under Euclidean geometry.</p><h2 id=\"part-3\">The chain rule connects operations</h2><p>For y=f(g(x)), dy/dx=f′(g(x))g′(x). A computation graph makes the intermediate values explicit. Backpropagation applies this rule backward, reusing intermediate derivatives. It computes gradients; an optimizer separately decides how to update parameters.</p><h2 id=\"part-4\">Jacobians and vector-Jacobian products</h2><p>For a vector output, the Jacobian contains derivatives of every output with respect to every input. Materializing it can be expensive. Reverse-mode differentiation propagates vector-Jacobian products from a scalar loss, which is efficient when there are many parameters but one objective.</p><h2 id=\"part-5\">Curvature and numerical checks</h2><p>Second derivatives describe how slopes change. Curvature makes one learning rate behave differently along different directions. Finite differences approximate gradients and can validate small implementations, but cancellation and step-size choice matter. Non-differentiable points often use an agreed subgradient.</p><h2 id=\"part-6\">Worked example</h2><p>Let prediction ŷ=wx and loss L=(wx−y)². For x=2, y=5, and w=1, the prediction is 2, loss is 9, and dL/dw=2(wx−y)x=−12. A step of 0.1 gives w=2.2 and the new loss is 0.36.</p><h2 id=\"part-7\">What goes wrong</h2><p>A negative gradient does not guarantee a finite step decreases the loss. Detaching a tensor or converting it to a Python number can break the computation graph.</p><div class=\"exercise\"><span class=\"eyebrow\">Check your understanding</span><p>Does backpropagation itself change weights?</p><details><summary>Reveal the explanation</summary><p>No. It computes derivatives. An optimizer uses them, potentially with momentum, adaptive statistics, weight decay, or other rules.</p></details></div><h2 id=\"part-8\">An operating model for calculus, gradients & the chain rule</h2><p>Backpropagation is an accounting method for responsibility. Each local derivative answers how an output would respond to a small input change; the chain rule sends that responsibility through the graph. It does not understand a task or select a learning rate. Those are modeling and optimization decisions made around it.</p><p><strong>The gotcha.</strong> A clean workflow diagram describes dependency order. It does not prove that any boundary is correct; each transition needs its own observable acceptance test.</p><h2 id=\"part-9\">Worked system: trace one error backwards</h2><p>Take a one-parameter model <code>ŷ = w × x</code> with one observation <code>x = 3</code>, target <code>y = 12</code>, and squared loss <code>L = (ŷ − y)²</code>. At <code>w = 2</code>, the prediction is 6 and the loss is 36. The derivative follows the graph: changing <code>w</code> changes <code>ŷ</code> by <code>x</code>; changing <code>ŷ</code> changes the loss by <code>2(ŷ−y)</code>. Thus <code>dL/dw = 2(6−12)×3 = −36</code>. A small positive update to <code>w</code> moves the prediction toward 12.</p><p>This miniature example contains the full backpropagation pattern. The loss contributes a local signal, each operation multiplies it by its local derivative, and parameter gradients accumulate where paths meet. It also shows why scale matters: if activations or derivatives become very large or very small across many layers, the update can explode or vanish. Gradient clipping, normalization, initialization, residual connections, and learning-rate schedules exist to keep this accounting process numerically usable.</p><h2 id=\"part-10\">Notebook: Check the quantity before trusting it</h2><p>For a new differentiable component, test the backward pass on a tiny input with finite differences. Compare directional derivatives rather than only individual elements when tensors are large.</p><pre><code>import numpy as np\nx = np.array([2., -1.])\nW = np.array([[1., 3.], [0., -2.]])\ny = W @ x\nprint(\"shape\", y.shape, \"value\", y)\n# Write units and axis meanings beside every array.</code></pre><div class=\"callout warning\"><strong>What to test next.</strong> 1. Forward pass: Compute intermediate values and the loss. 2. Local derivatives: Record how each operation changes nearby values. 3. Backward pass: Apply the chain rule from loss to parameters. 4. Update: Use the gradients with a controlled optimizer step.</div><p><strong>The gotcha.</strong> A small inspection script proves only the quantities it prints. Preserve units, shapes, versions, and the input fixture beside its output.</p><h2 id=\"part-11\">A rigorous way to practice</h2><ol><li><strong>Forward pass.</strong> Compute intermediate values and the loss.</li><li><strong>Local derivatives.</strong> Record how each operation changes nearby values.</li><li><strong>Backward pass.</strong> Apply the chain rule from loss to parameters.</li><li><strong>Update.</strong> Use the gradients with a controlled optimizer step.</li></ol><p>For a new differentiable component, test the backward pass on a tiny input with finite differences. Compare directional derivatives rather than only individual elements when tensors are large.</p><h2 id=\"part-12\">Verification questions for calculus, gradients & the chain rule</h2><div class=\"table-scroll\"><table><thead><tr><th>Boundary</th><th>Question</th><th>Evidence</th></tr></thead><tbody><tr><td>Forward pass</td><td>What would show that this boundary failed?</td><td>Compute intermediate values and the loss.</td></tr><tr><td>Local derivatives</td><td>What would show that this boundary failed?</td><td>Record how each operation changes nearby values.</td></tr><tr><td>Backward pass</td><td>What would show that this boundary failed?</td><td>Apply the chain rule from loss to parameters.</td></tr><tr><td>Update</td><td>What would show that this boundary failed?</td><td>Use the gradients with a controlled optimizer step.</td></tr></tbody></table></div><p><strong>The gotcha.</strong> Passing each component check separately does not prove the composed path preserves identity, units, ordering, or authority.</p><h2 id=\"part-13\">Derivatives are local linear models</h2><p>A derivative describes the best linear approximation near one point. For a scalar function it is a slope; for many inputs the gradient collects partial derivatives; for vector outputs the Jacobian records every local input-output sensitivity.</p><ul><li><strong>Directional derivatives test one perturbation.</strong> The dot product of a unit direction with the gradient predicts first-order change along that direction.</li><li><strong>The chain rule composes local maps.</strong> Reverse-mode automatic differentiation propagates one scalar loss backward through a graph and is efficient when parameters greatly outnumber outputs.</li><li><strong>Subgradients extend selected nonsmooth cases.</strong> ReLU and absolute value lack a unique derivative at a point, so libraries choose a valid convention that supports optimization.</li><li><strong>Finite differences provide an independent check.</strong> Compare a directional derivative with symmetric perturbations across several step sizes; steps that are too large incur approximation error, while tiny steps suffer cancellation.</li></ul><p><strong>The gotcha.</strong> A gradient is local. It does not identify a global optimum, a causal effect, or a safe step size.</p><h2 id=\"part-14\">Backpropagation is a memory and graph computation</h2><p>The forward pass creates intermediate values. The backward pass applies vector-Jacobian products in reverse dependency order. Frameworks retain enough graph state to compute those products.</p><ul><li><strong>Broadcasting reverses through reduction.</strong> A value broadcast across positions receives the sum of gradient contributions from those positions.</li><li><strong>In-place mutation can invalidate saved values.</strong> Autodiff needs the version used during the forward computation.</li><li><strong>Gradient accumulation is additive.</strong> Clear buffers deliberately or microbatches and repeated backward calls accumulate state.</li><li><strong>Checkpointing recomputes intermediates.</strong> It lowers activation memory and spends additional arithmetic.</li></ul><p><strong>The gotcha.</strong> A finite gradient can still be wrong because the loss, mask, reduction, or detached path encodes the wrong computation.</p>","headings":[{"id":"part-1","title":"How the pieces connect"},{"id":"part-2","title":"A derivative measures local change"},{"id":"part-3","title":"The chain rule connects operations"},{"id":"part-4","title":"Jacobians and vector-Jacobian products"},{"id":"part-5","title":"Curvature and numerical checks"},{"id":"part-6","title":"Worked example"},{"id":"part-7","title":"What goes wrong"},{"id":"part-8","title":"An operating model for calculus, gradients & the chain rule"},{"id":"part-9","title":"Worked system: trace one error backwards"},{"id":"part-10","title":"Notebook: Check the quantity before trusting it"},{"id":"part-11","title":"A rigorous way to practice"},{"id":"part-12","title":"Verification questions for calculus, gradients & the chain rule"},{"id":"part-13","title":"Derivatives are local linear models"},{"id":"part-14","title":"Backpropagation is a memory and graph computation"}],"words":1196,"minutes":7,"number":"07"}