const additions = {
  'bits-and-computers': `<h2>One bit pattern, several meanings</h2><p>A bit pattern has no intrinsic type. The consuming instruction decides whether it represents an unsigned integer, two's-complement integer, floating-point value, text, address, or flags.</p><ul><li><strong>Unsigned integers spend every pattern on magnitude.</strong> An n-bit value covers 0 through 2ⁿ−1 and usually wraps modulo 2ⁿ.</li><li><strong>IEEE 754 trades uniform spacing for dynamic range.</strong> Sign, exponent, and significand encode ordinary values plus NaN, infinities, signed zero, and subnormals.</li><li><strong>UTF-8 encodes Unicode code points as bytes.</strong> A visible grapheme may contain several code points, so byte length and character count differ.</li></ul><p><strong>The gotcha.</strong> Reinterpreting bytes changes meaning without converting value.</p><h2>Memory is a hierarchy</h2><p>Registers, caches, RAM, and storage trade capacity against latency and bandwidth. Dense scans exploit locality; pointer chasing and random page access often do not.</p><ul><li><strong>Caches move lines.</strong> Reading one field fetches nearby bytes, and unrelated writes on one line can create false sharing.</li><li><strong>Virtual memory translates and protects addresses.</strong> Page tables and translation caches add another locality boundary.</li><li><strong>Capacity includes live intermediates.</strong> Parameters coexist with activations, gradients, optimizer state, workspaces, padding, and copies.</li></ul><p><strong>The gotcha.</strong> A parameter file can fit while the running model fails at its memory peak.</p><h2>Instructions expose physical cost</h2><p>Compilers lower source into an instruction set. Pipelines overlap stages, out-of-order execution finds independent work, vector units operate on lanes, and atomic operations coordinate selected shared updates.</p><ul><li><strong>Dependencies set the parallel ceiling.</strong> More cores cannot accelerate a serial chain.</li><li><strong>Memory ordering belongs to correctness.</strong> An atomic counter does not protect a multi-step invariant.</li><li><strong>Formats preserve meaning across machines.</strong> Endianness, sizes, schema versions, units, and checksums need explicit contracts.</li></ul><p><strong>The gotcha.</strong> Successful parsing proves structure, not semantic compatibility.</p>`,
  'python-and-arrays': `<h2>Names, identity, and ownership</h2><p>Assignment binds a name to an object; it does not promise a copy. This matters when configurations, containers, iterators, and numerical views carry mutable state across experiments.</p><ul><li><strong>Identity differs from equality.</strong> <code>is</code> tests shared identity; <code>==</code> compares values.</li><li><strong>Mutable defaults persist across calls.</strong> Create per-call state inside the function.</li><li><strong>Shallow copies retain nested aliases.</strong> Define which layer owns mutation.</li><li><strong>Generators are consumable state.</strong> An exhausted iterator can produce an empty evaluation pass without an exception.</li></ul><p><strong>The gotcha.</strong> A fixed seed cannot control hidden mutation from an earlier run.</p><h2>Shape, dtype, strides, and offset define an array</h2><p>An ndarray is a view over a buffer. Shape gives logical extents; strides give byte steps; dtype determines representation and arithmetic.</p><ul><li><strong>Reshape may change only metadata.</strong> An incompatible layout forces a copy.</li><li><strong>Transpose usually changes strides.</strong> A later kernel may materialize contiguous storage.</li><li><strong>Basic slicing often returns a view.</strong> Advanced indexing commonly returns a copy.</li><li><strong>Broadcasting aligns axes from the right.</strong> It can avoid copying an input while allocating a huge output.</li></ul><p><strong>The gotcha.</strong> Compatible dimensions do not prove compatible axis meanings.</p><h2>Vectorization changes the cost model</h2><p>Array operations move loops into compiled kernels. Fusion avoids intermediates; batching amortizes overhead; reductions introduce ordering and numerical choices.</p><ul><li><strong>Benchmark synchronization boundaries.</strong> Reading an accelerator scalar may wait for queued work.</li><li><strong>Keep a slow oracle.</strong> Verify optimized operations against a transparent implementation on tiny inputs.</li><li><strong>Test invariants.</strong> Assert shape, finiteness, normalization, masking, and expected transformations.</li></ul><p><strong>The gotcha.</strong> A one-line expression can allocate several arrays and perform quadratic work.</p>`,
  'algorithms-and-complexity': `<h2>Growth covers work, space, communication, and span</h2><p>Complexity rules out impossible designs before profiling. Work counts operations; span counts the longest dependency chain; communication counts bytes, messages, and synchronization rounds.</p><ul><li><strong>Worst-case bounds protect tail behavior.</strong> Good average performance can coexist with an adversarial outage.</li><li><strong>Amortized analysis prices a sequence.</strong> Occasional resizing can still yield constant average append cost.</li><li><strong>Lower bounds expose changed assumptions.</strong> Beating a general bound usually changes keys, error tolerance, preprocessing, or the query.</li></ul><p><strong>The gotcha.</strong> Big-O hides constants and units; calculate bytes or operations at the intended scale.</p><h2>Data structures purchase operations with layout</h2><ul><li><strong>Arrays favor indexed scans and locality.</strong> Middle insertion requires movement.</li><li><strong>Hash tables favor exact lookup.</strong> They spend ordering and collision management.</li><li><strong>Trees preserve ordered operations.</strong> Balance maintains logarithmic depth.</li><li><strong>Graph storage follows density.</strong> Adjacency lists cost O(V+E); matrices cost O(V²).</li><li><strong>Indexes trade build cost for queries.</strong> Approximate indexes also trade recall for speed.</li></ul><p><strong>The gotcha.</strong> A lookup bound ignores iteration, cache misses, serialization, and operation mix.</p><h2>AI complexity starts with named tensor axes</h2><ul><li><strong>Dense attention couples positions quadratically.</strong> Doubling length creates four times the pairwise scores.</li><li><strong>Autoregressive decoding retains a serial dependency.</strong> Batching adds parallel work across requests.</li><li><strong>Retrieval separates recall from ranking.</strong> A fast index supplies candidates; a slower model reranks a bounded set.</li><li><strong>Checkpointing trades arithmetic for memory.</strong> Recomputed activations reduce stored state.</li></ul><p><strong>The gotcha.</strong> A faster kernel changes nothing when queues, transfers, retrieval, or tokenization dominate.</p>`,
  'systems-and-networking': `<h2>Processes, threads, and async tasks fail differently</h2><p>A process owns an address space. Threads share it. Async tasks usually share a thread and yield at suspension points. Each model defines isolation, communication, and cancellation boundaries.</p><ul><li><strong>Locks protect invariants.</strong> Holding one during I/O can serialize unrelated work.</li><li><strong>Async overlaps waiting.</strong> It does not make CPU-heavy work parallel.</li><li><strong>Cancellation requires cooperation.</strong> Stopping a caller does not undo a database commit, tool call, or accelerator kernel.</li></ul><p><strong>The gotcha.</strong> Tests must force contested schedules, cancellation, and exhaustion.</p><h2>Queues make overload explicit</h2><p>A queue absorbs bursts but cannot create capacity. When arrival exceeds sustainable service, waiting grows until rejection, timeout, or failure.</p><ul><li><strong>Bound queues.</strong> Shed work before memory exhaustion chooses the outcome.</li><li><strong>Measure queue time separately.</strong> Busy workers can coexist with unacceptable user latency.</li><li><strong>Propagate backpressure.</strong> A slow consumer does not help if producers retry into another buffer.</li><li><strong>Choose fairness.</strong> FIFO, priority, quotas, and aging distribute waiting differently.</li></ul><p><strong>The gotcha.</strong> Throughput can improve while p99 latency violates the promise.</p><h2>Networks create ambiguous outcomes</h2><p>A timeout cannot distinguish a slow server, lost request, lost response, or work committed after the deadline.</p><ul><li><strong>TCP is an ordered byte stream.</strong> Applications define messages and commits.</li><li><strong>Retries need deadlines, jitter, and idempotency.</strong> Deduplication must share a transaction with the effect.</li><li><strong>Ordering has a scope.</strong> One connection may preserve order while the whole system does not.</li><li><strong>Trace the complete request.</strong> Separate queue, retrieval, prefill, decode, tool, and commit time.</li></ul><p><strong>The gotcha.</strong> Exactly-once behavior usually combines at-least-once delivery with transactional deduplication.</p>`,
};

export function deepenComputing(chapter) {
  return additions[chapter.id] ? {...chapter, html: chapter.html + additions[chapter.id]} : chapter;
}
