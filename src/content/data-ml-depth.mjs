const additions = {
  'data-lifecycle': `<h2>A dataset contract starts before the first row arrives</h2><p>Data quality is only meaningful relative to a decision. Before collection, write the prediction or analysis unit, the actor who will use the result, the time at which that actor needs it, the target outcome, and the consequence of each error. A support-ticket priority model, for example, may receive one initial customer message and must recommend a queue before an agent replies. A later message, final resolution, or satisfaction survey may be useful for labeling but cannot be an input. This distinction between <strong>prediction time</strong> and <strong>label availability</strong> is the first protection against leakage.</p><p>The contract should give every field a semantic definition: entity key, event time, processing time, unit, allowed range, missing-value meaning, producer, retention rule, and permitted uses. A column named <code>duration</code> is not a contract until it says whether it means seconds or milliseconds, started or completed time, and whether null means unknown, not applicable, or not yet observed. Schema validation catches a string where a number belongs; semantic validation catches a quantity that is numerically valid but means something else.</p><div class="table-scroll"><table><thead><tr><th>Contract question</th><th>Example answer</th><th>Failure if omitted</th></tr></thead><tbody><tr><td>What is one record?</td><td>One ticket at its first customer message</td><td>Related conversations leak across splits</td></tr><tr><td>When is it known?</td><td>At queue assignment</td><td>Future resolution becomes a feature</td></tr><tr><td>Who produces it?</td><td>Versioned helpdesk export</td><td>Silent producer changes alter meaning</td></tr><tr><td>What does missing mean?</td><td>Customer did not supply a product ID</td><td>Imputation hides a process change</td></tr></tbody></table></div><p><strong>The gotcha.</strong> A data contract is not a document written once. It is an executable boundary: producers validate on write, consumers validate on read, and changes require a versioned decision about compatibility.</p>

<h2>Collection and labels are measurement systems</h2><p>Observed data is not the world; it is the trace left by an instrument, workflow, and policy. Clicks depend on what was shown. Fraud labels may arrive only after an investigation. Medical outcomes may be absent for people who did not return to a clinic. Moderation labels reflect both user behavior and the current enforcement policy. Treating every row as an independent, complete ground truth obscures selection, delay, and disagreement.</p><p>Label instructions need the same specificity as an API. Define the evidence an annotator may use, the label taxonomy, how ambiguity is represented, whether multiple labels are allowed, and how adjudication works. Sample disagreement deliberately rather than treating it as noise: disagreement can reveal underspecified categories, culturally dependent interpretation, or cases where the product should abstain. Keep annotator or process version separate from the label value so later analysis can distinguish a changed population from a changed rubric.</p><p>Sampling also changes the claim a model can support. Convenience samples often overrepresent active users, well-instrumented regions, common languages, or easy cases. A balanced training sample can help optimization but does not make a deployment population balanced. Preserve the inclusion mechanism and population denominator so metrics can be reweighted or sliced honestly.</p>

<h2>Quality checks must follow the record through the pipeline</h2><p>Run structural checks close to ingestion: schema, parse errors, type, range, timestamp ordering, key uniqueness, referential integrity, and volume changes. Then run content checks on representative raw records and transformed outputs. Distribution summaries are useful alarms, not proof of correctness. A stable mean can hide a swapped category mapping; a valid JSON document can contain truncated text; an apparent duplicate can be either a retry worth collapsing or a repeated event that matters.</p><p>Build a small reviewed fixture set that includes ordinary records, missing values, late arrivals, malformed encodings, duplicate-like cases, rare classes, and sensitive edge cases. Every transformation should have an expected result for these fixtures. This makes semantic regressions visible when a parser, normalization rule, or upstream producer changes. It also gives reviewers something concrete to inspect instead of relying on aggregate dashboards.</p><p>Quality monitoring should separate <strong>data-health signals</strong> from <strong>task signals</strong>. Missingness, cardinality, freshness, and schema drift describe the pipeline. Label prevalence, calibration, error slices, and outcome delay describe whether the model’s problem has changed. A clean pipeline can deliver a dataset that is no longer useful, and a high model score can conceal a broken data feed if the evaluation set is stale.</p>

<h2>Split design defines the generalization claim</h2><p>A random row split estimates performance on future rows drawn independently from the same process. That assumption fails when rows share a person, device, document family, organization, session, or template. Split by the unit that could cause memorization. For time-dependent work, train on data available before a cutoff and evaluate on later data; then inspect whether every feature, join, and label respects that cutoff. A temporal split that uses a later dimension-table snapshot can still leak.</p><p>Freeze split membership as an artifact: record IDs or stable hashes, split rule, cutoff, group key, code version, and dataset manifest. Reproducibility is not merely rerunning the same query. It is being able to show which observations supported a comparison and why an observation belonged there. When labels are delayed, explicitly model the censoring window so records do not appear negative simply because their outcome has not had time to arrive.</p><p><strong>The gotcha.</strong> Deduplication belongs before or inside the split decision, never as an afterthought. Removing near-duplicates after a random split may leave one member of a template family in train and another in test, producing a plausible but inflated score.</p>

<h2>Lineage is a graph of records, transformations, and artifacts</h2><p>Lineage should answer a concrete incident question: “Why did this prediction use this value?” Trace from one prediction to its feature values, source records, transformation versions, model release, and policy; trace in the other direction from one source correction to the feature tables, embeddings, training runs, indexes, evaluations, and deployed releases it affected. Paths alone are insufficient. Record the content identity of each dataset snapshot through manifests or immutable hashes, not only a mutable storage location.</p><p>Keep event time and processing time separately. Backfills, retries, late-arriving events, and corrected source records are normal operational behavior. Without both clocks, an analyst cannot tell whether a model had information at decision time or whether a warehouse merely received it later. The same discipline applies to derived artifacts: an embedding, feature vector, cached retrieval chunk, and model checkpoint each need provenance, owner, and lifecycle state.</p><p>Deletion and access requests must follow those edges. Deleting a raw document while retaining its chunks in a vector index, features in an offline table, examples in a benchmark export, and content in a debug log does not complete the lifecycle. The practical goal is not perfect omniscience; it is a tested impact path and an explicit policy for what is retrained, reindexed, expired, or retained under a documented exception.</p>

<h2>A review protocol before training</h2><ol class="practice-sequence"><li><strong>Read raw examples.</strong> Review ordinary, rare, malformed, and disputed records with the people who understand the producing workflow.</li><li><strong>Write the time boundary.</strong> For every feature, state its event time, processing time, and whether it exists at the decision moment.</li><li><strong>Audit representation.</strong> Compare the collected sample with the intended population, including language, geography, device, source, and outcome coverage.</li><li><strong>Freeze an evaluation manifest.</strong> Save IDs, split logic, code revision, transformation versions, and the expected label-delay window.</li><li><strong>Exercise an incident path.</strong> Correct or remove one source record and verify which downstream artifacts are identified and updated.</li></ol><p>A dataset is ready for modeling when a reviewer can explain one record end to end, reproduce the split, name the important exclusions, and identify what a source correction changes. Those are stronger conditions than “the file loaded” or “the dashboard is green.”</p>`,
  'features-and-pipelines': `<h2>Training-serving skew is a contract failure</h2><p>A feature has a definition, timestamp, entity key, unit, missing-value rule, and availability boundary. Training and serving must implement the same contract.</p><ul><li><strong>Fit transformations on training data.</strong> Scalers, imputers, vocabularies, encoders, selectors, and learned embeddings otherwise leak evaluation information.</li><li><strong>Join as of prediction time.</strong> A correct entity join can still leak a future outcome through a later snapshot.</li><li><strong>Unknown categories need explicit behavior.</strong> Rejecting, mapping to an unknown token, and hashing create different failure modes.</li><li><strong>Validate semantics beside schema.</strong> A numeric field can pass type checks while changing from seconds to milliseconds.</li></ul><p><strong>The gotcha.</strong> Reusing feature code does not guarantee reuse of source snapshots, lookup state, or execution order.</p>`,
  'search-and-retrieval': `<h2>Retrieval is a measured cascade</h2><p>Ingestion creates searchable units; candidate generation seeks recall; reranking spends more computation on a small set; answer construction cites or abstains.</p><ul><li><strong>Chunking defines retrievable evidence.</strong> Chunks that are too small lose context; chunks that are too large dilute matching and consume context budget.</li><li><strong>Lexical and dense retrieval fail differently.</strong> Exact identifiers favor token matching; paraphrase favors learned geometry. Hybrid fusion needs score normalization or rank-based combination.</li><li><strong>Metadata filters enforce scope before ranking.</strong> Post-filtering can return too few authorized candidates or leak counts and snippets.</li><li><strong>Reranking cannot recover an absent passage.</strong> Report candidate recall separately from final ranking and answer quality.</li></ul><p><strong>The gotcha.</strong> A cited passage can be current and relevant yet fail to entail the generated claim.</p>`,
  'supervised-learning': `<h2>The label is a measurement process</h2><p>A target can be delayed, censored, noisy, policy-dependent, or available only for selected cases. Model quality cannot exceed the meaning and coverage of that measurement.</p><ul><li><strong>Separate prediction time from label time.</strong> Features created after the decision leak the answer.</li><li><strong>Choose a split that represents deployment.</strong> Time, entity, geography, device, and source boundaries often matter more than random balance.</li><li><strong>Start with a simple baseline.</strong> It reveals whether complexity improves the decision or only absorbs leakage and tuning effort.</li><li><strong>Convert scores into policy.</strong> Thresholds depend on error costs, review capacity, calibration, abstention, and recourse.</li></ul><p><strong>The gotcha.</strong> A better ranking metric can make operations worse at the threshold the product actually uses.</p>`,
  'trees-and-ensembles': `<h2>Trees partition data; ensembles control their instability</h2><p>A decision tree repeatedly chooses a feature condition that improves an impurity or loss objective. Deep trees adapt closely to data and can change substantially after a small sample perturbation.</p><ul><li><strong>Random forests reduce variance.</strong> Bootstrap samples and feature subsampling decorrelate trees, then averaging stabilizes prediction.</li><li><strong>Boosting follows residual structure.</strong> Sequential learners correct current error and require learning-rate, depth, and stopping controls.</li><li><strong>Missing values and categories are algorithm-specific.</strong> Native routing, encoding, and imputation produce different splits and leakage risks.</li><li><strong>Importance is not causality.</strong> Gain, split count, permutation, and SHAP answer different model questions and react differently to correlated features.</li></ul><p><strong>The gotcha.</strong> A proxy feature can dominate prediction precisely because the ensemble is good at exploiting it.</p>`,
  'unsupervised-learning': `<h2>Structure appears relative to a representation</h2><p>Clustering, dimensionality reduction, and anomaly detection optimize a geometry selected through features, scaling, distance, neighborhood, and density assumptions.</p><ul><li><strong>K-means assumes Euclidean compactness.</strong> It prefers roughly spherical groups and its mean is sensitive to scale and outliers.</li><li><strong>PCA preserves variance, not task relevance.</strong> High-variance directions can be nuisance; rare low-variance directions can carry the decision.</li><li><strong>Density methods define local normality.</strong> Neighborhood size changes whether a point looks isolated.</li><li><strong>Embeddings inherit an objective.</strong> A visually clean projection can distort global distance and cluster separation.</li></ul><p><strong>The gotcha.</strong> Cluster labels are hypotheses for investigation, not discovered natural kinds.</p>`,
  'evaluation-basics': `<h2>An evaluation is an experiment with a release decision</h2><p>The dataset, sampling frame, protocol, metric, baseline, uncertainty, and veto rule jointly define what an evaluation can support.</p><ul><li><strong>Protect the test set from selection.</strong> Prompt tuning, checkpoint choice, feature design, and repeated error inspection all adapt to examples.</li><li><strong>Report denominators and intervals.</strong> A slice score without sample count can turn one example into a confident-looking percentage.</li><li><strong>Measure the operating point.</strong> Calibration, threshold errors, abstention, latency, cost, and human escalation connect model scores to a service.</li><li><strong>Use counterexamples and shifts.</strong> Aggregate IID accuracy misses temporal change, adversarial inputs, unsupported requests, and rare high-cost errors.</li></ul><p><strong>The gotcha.</strong> A benchmark can be measured perfectly and still lack construct validity for the claimed capability.</p>`,
  'neural-networks': `<h2>Capacity comes from composition</h2><p>A neural network alternates learned affine maps with nonlinear operations. The nonlinearities prevent the entire stack from collapsing into one matrix and let depth reuse intermediate features.</p><ul><li><strong>Initialization controls signal scale.</strong> Poor variance propagation saturates activations or makes gradients vanish and explode.</li><li><strong>Residual paths create short gradient routes.</strong> They support deep optimization but do not remove normalization, precision, or objective failures.</li><li><strong>Normalization changes interactions.</strong> Batch, layer, and RMS normalization use different axes and create different training-serving behavior.</li><li><strong>Regularization encodes invariances and capacity preferences.</strong> Augmentation, dropout, decay, and early stopping are not interchangeable.</li></ul><p><strong>The gotcha.</strong> A network that cannot overfit a tiny batch has an implementation or data-path problem, not a generalization problem.</p>`,
};

export function deepenDataAndML(chapter) {
  return additions[chapter.id] ? {...chapter, html: chapter.html + additions[chapter.id]} : chapter;
}
