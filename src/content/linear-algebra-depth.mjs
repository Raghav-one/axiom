const section = (title, intro, body, gotcha) => `<h2>${title}</h2><p>${intro}</p>${body}<div class="gotcha"><span>THE GOTCHA</span><p>${gotcha}</p></div>`;
const code = source => `<pre><code>${source.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}</code></pre>`;

const vectorMap = `<figure class="concept-diagram" aria-labelledby="vector-map-title"><figcaption id="vector-map-title"><span>GEOMETRIC MAP</span><strong>One vector, three views</strong></figcaption><div class="vector-views"><div><b>Abstract object</b><span>v ∈ V</span><small>obeys vector-space rules</small></div><i>→ choose a basis →</i><div><b>Coordinates</b><span>[v]ᵦ = (2, −1)</span><small>numbers relative to B</small></div><i>→ choose an inner product →</i><div><b>Geometry</b><span>‖v‖, angle, projection</span><small>length and orthogonality</small></div></div><p>Coordinates and geometry are additional structure. Changing basis changes the coordinate list while the represented vector remains the same.</p></figure>`;

const attentionMap = `<figure class="concept-diagram" aria-labelledby="attention-map-title"><figcaption id="attention-map-title"><span>TENSOR CONTRACT</span><strong>One attention head, with every axis named</strong></figcaption><div class="attention-flow"><div><b>X</b><span>[tokens, model]</span></div><i>× Wq →</i><div><b>Q</b><span>[tokens, head]</span></div><i>× Kᵀ →</i><div><b>scores</b><span>[tokens, tokens]</span></div><i>softmax →</i><div><b>weights</b><span>[query, key]</span></div><i>× V →</i><div><b>output</b><span>[tokens, head]</span></div></div><p>The inner dimension is summed away at each product. The remaining axes state which object each output element describes.</p></figure>`;

const vocabulary = `<div class="table-scroll"><table><thead><tr><th>Object</th><th>What it represents</th><th>Operation that matters</th><th>Failure to avoid</th></tr></thead><tbody><tr><td>scalar</td><td>one element of a field</td><td>addition and multiplication</td><td>forgetting units or precision</td></tr><tr><td>vector</td><td>one element of a vector space</td><td>linear combination</td><td>treating coordinates as intrinsic meaning</td></tr><tr><td>matrix</td><td>a linear map after bases are chosen</td><td>matrix-vector product</td><td>reading it only as a grid of numbers</td></tr><tr><td>subspace</td><td>a set closed under vector operations</td><td>span and projection</td><td>confusing a translated affine set with a subspace</td></tr><tr><td>basis</td><td>independent vectors spanning a space</td><td>coordinate representation</td><td>comparing coordinates across different bases</td></tr><tr><td>rank</td><td>independent directions preserved</td><td>dimension of image</td><td>using an exact rank test on noisy floats</td></tr><tr><td>tensor array</td><td>multidimensional coordinates in software</td><td>indexed contraction</td><td>leaving axis semantics implicit</td></tr></tbody></table></div>`;

const html = `<p>Linear algebra is the language of representations and transformations. An image, token sequence, parameter set, batch of examples, or probability table becomes useful to a machine-learning system after it is arranged into coordinates and acted on by structured maps. The subject is not matrix arithmetic alone. It explains which directions exist, which information a transformation preserves, how geometry is defined, and when a numerical answer deserves trust.</p>` +
section('Vectors are objects before they are coordinate lists', 'A vector is an element of a set in which addition and scalar multiplication obey the vector-space rules. The familiar arrow in two dimensions is one example. Polynomials, signals, images, feature rows, and model parameters can also form vector spaces once their legal combinations and scalars are specified.', `<p><strong>Linear combinations generate reachable objects.</strong> Given vectors v₁,…,vₖ and scalars a₁,…,aₖ, the expression a₁v₁+⋯+aₖvₖ is a linear combination. The <strong>span</strong> contains every object reachable this way. Asking whether a target lies in a span is asking whether the available directions can represent it exactly.</p><p><strong>Independence removes redundant directions.</strong> A set is linearly independent when the zero vector can be formed only by setting every coefficient to zero. If one vector can be reconstructed from the others, it adds no new direction. Dependence is about the represented space, not whether two coordinate rows happen to look visually similar.</p><p><strong>A basis provides unique coordinates.</strong> A basis is both independent and spanning. Once a basis B=(b₁,…,bₙ) is chosen, each vector has one coordinate list [v]ᵦ whose entries state how much of each basis vector forms v. A different basis gives different coordinates for the same underlying object.</p><p><strong>Subspaces capture constrained variation.</strong> A subspace contains zero and remains closed under addition and scalar multiplication. Solutions to homogeneous linear equations form subspaces. A plane offset away from the origin is an affine set rather than a subspace; neural layers Wx+b are affine because the bias translates the origin.</p>${vocabulary}${vectorMap}`, 'A coordinate axis has no invariant semantic meaning by itself. Comparing the third embedding coordinate across independently trained models, or after an arbitrary basis rotation, usually compares representation choices rather than stable concepts.') +
section('Matrices are maps between spaces', 'A matrix represents a linear transformation after input and output bases are chosen. Reading it as a map explains multiplication, shape, rank, and composition more clearly than memorizing entry-wise rules.', `<p>For A ∈ ℝᵐˣⁿ and x ∈ ℝⁿ, the product y=Ax lies in ℝᵐ. Each <strong>row</strong> of A forms one dot product with x and therefore asks one linear question of the input. Each <strong>column</strong> is the output produced by one input basis direction. The output is also a linear combination of columns weighted by the coordinates of x.</p><div class="derivation"><span>ENTRY-WISE VIEW</span><p>yᵢ = Σⱼ Aᵢⱼxⱼ</p><p>Input index j is summed away. Output index i remains. In tensor code, this sentence is the shape contract.</p></div><p><strong>Composition becomes matrix multiplication.</strong> If A maps input coordinates into an intermediate space and B maps that space into an output space, BA applies A first and B second. The inner dimensions must agree because they name the same intermediate coordinate system. Order matters: AB and BA may have different shapes, and even when both exist they generally express different maps.</p><p><strong>Rank counts preserved independent directions.</strong> The column space contains every output A can produce. The null space contains every input erased to zero. Rank is the dimension of the column space and equals the dimension of the row space. Rank-nullity states that input dimension equals rank plus nullity.</p><p><strong>Transpose reverses the direction under the standard inner product.</strong> Aᵀ maps output-space coordinates back toward the input space and satisfies ⟨Ax,y⟩=⟨x,Aᵀy⟩. This identity explains why transposes appear in gradients, least squares, covariance, and backpropagation.</p>${code(`import numpy as np
A = np.array([[1., 2., 0.],
              [0., 1., 3.]])   # [output=2, input=3]
x = np.array([4., -1., 2.])    # [input=3]
y = A @ x                       # [output=2]
assert y.shape == (2,)
assert np.allclose(y, [2., 5.])
print("rank", np.linalg.matrix_rank(A))`)}`, 'Compatible dimensions prove that an operation is syntactically legal. They do not prove the shared axis has the same semantic meaning. Label batch, time, channel, feature, head, and class axes before trusting a product.') +
section('Inner products create geometry', 'A bare vector space supports combination but does not yet define length or angle. An inner product adds that geometry. The ordinary dot product is one choice; weighted inner products and learned representations create other geometries.', `<p>The Euclidean inner product xᵀy multiplies corresponding coordinates and sums them. It defines the norm ‖x‖₂=√(xᵀx) and, for nonzero vectors, cos θ=(xᵀy)/(‖x‖₂‖y‖₂). Orthogonal vectors have zero inner product. These relationships extend the Pythagorean theorem and make projection possible.</p><p><strong>Norm choice changes the neighborhood.</strong> The L1 norm sums absolute coordinates, L2 uses Euclidean length, and L∞ keeps the largest absolute coordinate. Their unit balls have different shapes, so nearest points, regularization, and robustness regions differ. A claim about “small perturbations” is incomplete without a norm and scale.</p><p><strong>Cosine similarity discards magnitude.</strong> Normalizing by both norms keeps directional alignment and removes scale. This helps when direction carries semantic information and magnitude is nuisance variation. It hurts when magnitude encodes confidence, frequency, intensity, or uncertainty. Zero vectors require explicit handling.</p><p><strong>Projection separates represented signal from residual.</strong> Projecting b onto a subspace finds the point in that subspace nearest to b under the chosen inner product. The residual is orthogonal to every allowed direction. This is the geometric foundation of least squares and of decomposing signal into explained and unexplained components.</p><div class="derivation"><span>PROJECTION ONTO ONE DIRECTION</span><p>projᵤ(v) = (uᵀv / uᵀu)u</p><p>The scalar coefficient measures alignment with u; multiplying by u returns the represented component in the original space.</p></div><p><strong>Gram matrices collect pairwise geometry.</strong> XᵀX contains feature-feature inner products; XXᵀ contains example-example inner products. Kernel methods replace an explicit coordinate map with pairwise inner products. Forming a Gram matrix can square the larger axis and amplify conditioning problems.</p>`, 'Similarity is always similarity under a representation, preprocessing rule, scale, and inner product. Embedding proximity does not by itself establish identity, truth, causal relationship, fairness, or permission to act.') +
section('Linear systems expose existence, uniqueness, and sensitivity', 'The equation Ax=b asks whether an input x maps to the target b. The answer can be unique, absent, or non-unique. Those cases follow from the geometry of the column and null spaces, not from whether a solver returns a floating-point array.', `<p>If b lies in the column space of A, at least one exact solution exists. If the null space contains only zero, that solution is unique. A square matrix is invertible exactly when it has full rank, but explicitly computing A⁻¹ is rarely the right numerical way to solve a system.</p><p><strong>Elimination changes representation while preserving solutions.</strong> Row operations reduce a system toward triangular form. Pivoting chooses numerically safer leading elements and records row exchanges. Forward elimination plus back substitution costs finite work and exposes singularity more directly than symbolic inversion.</p><p><strong>Overdetermined systems lead to least squares.</strong> When more equations than unknowns conflict, no exact x may exist. Least squares minimizes ‖Ax−b‖₂². At the optimum, the residual is orthogonal to every column of A, yielding AᵀAx=Aᵀb.</p><p><strong>Normal equations can damage numerical information.</strong> Forming AᵀA squares the condition number. QR factorization solves least squares through an orthogonal transformation without that squaring. SVD costs more but handles rank deficiency and exposes weak directions explicitly.</p><p><strong>Regularization changes the objective.</strong> Ridge regression minimizes ‖Ax−b‖₂²+λ‖x‖₂². It shrinks poorly determined directions and can reduce variance, but it introduces bias. The regularization strength belongs to the model-selection protocol, not to algebraic convenience.</p>${code(`import numpy as np
A = np.array([[1., 1.], [1., 2.], [1., 3.]])
b = np.array([1., 2., 2.])
x, residuals, rank, singular = np.linalg.lstsq(A, b, rcond=None)
print("solution", x)
print("residual norm", np.linalg.norm(A @ x - b))
print("rank", rank, "singular values", singular)
# Prefer lstsq/solve to inv(A) @ b.`)}`, 'A solver completing without an exception does not prove the inferred parameters are stable. Inspect rank, singular values, condition, residuals, units, and sensitivity to plausible input perturbations.') +
section('Decompositions reveal structure and provide algorithms', 'A matrix decomposition rewrites one transformation as factors with simpler geometry or computation. Factorizations support solving, compression, whitening, dimensionality reduction, and diagnosis because they isolate orientation, scale, and dependency.', `<p><strong>LU separates elimination from substitution.</strong> With pivoting, PA=LU writes a matrix as lower- and upper-triangular factors plus row permutation. Reusing the factorization makes multiple right-hand sides cheaper. Sparse variants must control fill-in, where elimination creates stored nonzeros absent from the original matrix.</p><p><strong>QR separates an orthonormal basis from coordinates.</strong> A=QR gives Q with orthonormal columns and upper-triangular R. Q preserves Euclidean lengths within its represented space. QR is a reliable least-squares route and underlies methods that repeatedly build stable bases.</p><p><strong>Eigenvectors identify invariant directions of a square map.</strong> Av=λv means v keeps its direction while scaling by λ. Not every matrix has a complete well-conditioned eigenbasis. Symmetric real matrices are especially well behaved: they have real eigenvalues and an orthonormal eigenbasis.</p><p><strong>SVD works for every rectangular matrix.</strong> A=UΣVᵀ maps orthonormal input directions V into orthonormal output directions U with nonnegative singular values. Rank equals the count of nonzero singular values. The pseudoinverse reciprocates sufficiently nonzero singular values and discards null directions.</p><p><strong>Truncated SVD gives an optimal low-rank approximation.</strong> Keeping the largest k singular values produces the closest rank-k matrix under common norms. This compresses a map by retaining directions that carry the most matrix energy. Downstream usefulness still needs task evaluation because low-energy directions can contain rare essential behavior.</p><div class="derivation"><span>LOW-RANK STORAGE</span><p>A dense m×n matrix stores mn values. A rank-k factorization stores approximately k(m+n) values.</p><p>Compression helps when k(m+n) is much smaller than mn and the discarded singular directions are acceptable.</p></div>`, 'A decomposition is exact or optimal under stated mathematical conditions and norms. That does not make its interpretation unique or task-relevant. Repeated eigenvalues, sign flips, rotations inside degenerate subspaces, and noisy near-zero singular values complicate component stories.') +
section('Conditioning separates mathematical difficulty from algorithm quality', 'Numerical linear algebra operates with rounded inputs and finite precision. Conditioning describes how sensitive the mathematical problem is to small perturbations. Stability describes whether an algorithm adds much more error than that unavoidable sensitivity.', `<p>For an invertible matrix under a compatible norm, κ(A)=‖A‖‖A⁻¹‖. Under the Euclidean norm this is the ratio of largest to smallest singular value. A large condition number means some input or rounding perturbations can be strongly amplified. No clever solver can recover information the problem geometry barely preserves.</p><p><strong>Residual and solution error answer different questions.</strong> A small residual ‖Ax̂−b‖ shows that the computed x̂ nearly satisfies the supplied equation. In an ill-conditioned system, many substantially different x values can have small residuals. Report both residual and a sensitivity or uncertainty analysis.</p><p><strong>Scaling changes numerical behavior.</strong> Features measured in dollars, microseconds, and probabilities can create wildly different column magnitudes. Standardization or physically meaningful nondimensionalization can improve optimization and interpretation. Scaling also changes regularization and distance, so it must be fitted and versioned as part of the model.</p><p><strong>Floating-point order matters.</strong> Matrix kernels accumulate many products. Precision of inputs, products, and accumulators may differ. Parallel reductions combine partial sums in implementation-dependent trees. Mixed-precision training works by choosing which quantities tolerate approximation and which need higher range or precision.</p><p><strong>Structure should guide the algorithm.</strong> Symmetry, positive definiteness, sparsity, banded form, orthogonality, and low rank enable faster or more stable methods. Converting a sparse matrix to dense form or ignoring symmetry can spend orders of magnitude more memory and work.</p>${code(`import numpy as np
A = np.array([[1., 1.], [1., 1.000001]])
b = np.array([2., 2.000001])
x = np.linalg.solve(A, b)
print("x", x)
print("condition", np.linalg.cond(A))
print("residual", np.linalg.norm(A @ x - b))

# Perturb the observed target, then inspect solution sensitivity.
bp = b + np.array([0., 1e-7])
print("perturbed x", np.linalg.solve(A, bp))`)}`, 'Treating every failure as “floating-point noise” hides the distinction between a badly conditioned problem, an unstable algorithm, an incorrect implementation, and a wrong model. Diagnose them separately.') +
section('Tensor operations are linear algebra with named axes', 'Machine-learning libraries store multidimensional arrays and apply linear algebra across selected axes. The term tensor in software often refers to this array plus dtype, device, shape, strides, and gradient metadata. Correct code requires both algebraic and semantic axis contracts.', `<p>A batch matrix product can be written with shapes rather than indices: X [batch, tokens, input] multiplied by W [input, output] produces Y [batch, tokens, output]. The input axis is contracted. Batch and token axes remain. Broadcasting may reuse W without physically copying it, but the output still contains every resulting element.</p><p><strong>Einstein notation makes contraction explicit.</strong> Repeated indices are summed; unrepeated indices remain. The equation yᵢ=Aᵢⱼxⱼ expresses a matrix-vector product. Attention scores sᵦₕᵢⱼ=qᵦₕᵢₖkᵦₕⱼₖ sum over head feature k while retaining batch b, head h, query position i, and key position j.</p><p><strong>Reshape and transpose change different things.</strong> Reshape reinterprets index grouping when storage layout permits. Transpose permutes axes and commonly changes strides without moving data. A later kernel may require contiguous layout and materialize a copy, so a cheap-looking view can move bytes at its consumer.</p><p><strong>Automatic differentiation applies adjoint maps.</strong> A local backward rule maps an output sensitivity into input sensitivities. For y=Wx, the gradient with respect to x uses Wᵀ, while the parameter gradient is an outer product accumulated over examples. Shape reasoning often catches a wrong backward derivation before numerical testing.</p><p><strong>Attention combines projections and data-dependent mixing.</strong> Learned matrices create queries, keys, and values. Query-key products form a score matrix, scaling controls magnitude, masking removes illegal interactions, softmax normalizes rows, and weights combine values. Each step has a distinct axis meaning and failure mode.</p>${attentionMap}${code(`import numpy as np
tokens, model, head = 4, 6, 3
X = np.random.default_rng(0).normal(size=(tokens, model))
Wq = np.random.default_rng(1).normal(size=(model, head))
Wk = np.random.default_rng(2).normal(size=(model, head))
Wv = np.random.default_rng(3).normal(size=(model, head))
Q, K, V = X @ Wq, X @ Wk, X @ Wv
scores = Q @ K.T / np.sqrt(head)
weights = np.exp(scores - scores.max(axis=-1, keepdims=True))
weights /= weights.sum(axis=-1, keepdims=True)
output = weights @ V
assert scores.shape == (tokens, tokens)
assert output.shape == (tokens, head)`)}`, 'A tensor expression can execute, return the expected rank, and remain semantically wrong. Broadcasting a batch axis against a channel axis is legal arithmetic. Name every axis in equations, assertions, fixtures, and interfaces.') +
section('A practical method for trustworthy matrix code', 'Reliable linear-algebra work connects four views: the abstract spaces, coordinate shapes, numerical algorithm, and physical implementation. Skipping any view creates bugs that another view cannot expose.', `<ol class="practice-sequence"><li><strong>Write the map in words.</strong> State what the input coordinates mean, what space the output belongs to, which axis is aggregated, and which invariants should hold.</li><li><strong>Annotate every shape and dtype.</strong> Put semantic axis names beside formulas and code. Record device, precision, layout, and units at interfaces.</li><li><strong>Construct a tiny exact case.</strong> Use values small enough to calculate by hand. Compare a transparent loop or symbolic result with the vectorized implementation.</li><li><strong>Choose an algorithm from structure.</strong> Use solve rather than inverse, QR or SVD for least squares when conditioning matters, and sparse methods when storage structure is real.</li><li><strong>Measure residuals and invariants.</strong> Check symmetry, normalization, orthogonality, conservation, rank, and original-equation residuals rather than only inspecting an output array.</li><li><strong>Perturb the inputs.</strong> Small changes reveal sensitivity, unstable thresholds, and near-degenerate components. Repeat under the dtypes and scales used in production.</li><li><strong>Profile movement and synchronization.</strong> Large matrix operations may be dominated by layout conversion, host-device transfer, workspace, or reduction. Time representative shapes at explicit synchronization boundaries.</li></ol><div class="table-scroll"><table><thead><tr><th>Question</th><th>Evidence</th></tr></thead><tbody><tr><td>Is the map semantically correct?</td><td>Named axes, units, tiny hand-computed fixture</td></tr><tr><td>Is the problem well determined?</td><td>Rank, singular values, condition, identifiability assumptions</td></tr><tr><td>Is the computed result accurate?</td><td>Residual, tolerance, higher-precision or alternate solver comparison</td></tr><tr><td>Is the implementation efficient?</td><td>Representative profile including allocation, layout, transfer, synchronization</td></tr><tr><td>Does an approximation preserve utility?</td><td>Downstream metrics and rare-case analysis, not reconstruction alone</td></tr></tbody></table></div>`, 'A passing shape assertion is a starting point. Trust requires semantic fixtures, numerical diagnostics, and task evidence. Fast, finite, correctly shaped numbers can still encode the wrong transformation.') ;

export function deepenLinearAlgebra(chapter) {
  if (chapter.id !== 'linear-algebra') return chapter;
  return {
    ...chapter,
    summary: 'Build linear algebra as the study of spaces, transformations, geometry, decompositions, conditioning, and named tensor axes used throughout machine learning.',
    sources: [
      ['Mathematics for Machine Learning', 'https://mml-book.github.io/'],
      ['NumPy linear algebra reference', 'https://numpy.org/doc/stable/reference/routines.linalg.html'],
      ['Dive into Deep Learning: linear algebra', 'https://d2l.ai/chapter_preliminaries/linear-algebra.html']
    ],
    html
  };
}
