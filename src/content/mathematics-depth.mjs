const additions = {
  'calculus-and-gradients': `<h2>Derivatives are local linear models</h2><p>A derivative describes the best linear approximation near one point. For a scalar function it is a slope; for many inputs the gradient collects partial derivatives; for vector outputs the Jacobian records every local input-output sensitivity.</p><ul><li><strong>Directional derivatives test one perturbation.</strong> The dot product of a unit direction with the gradient predicts first-order change along that direction.</li><li><strong>The chain rule composes local maps.</strong> Reverse-mode automatic differentiation propagates one scalar loss backward through a graph and is efficient when parameters greatly outnumber outputs.</li><li><strong>Subgradients extend selected nonsmooth cases.</strong> ReLU and absolute value lack a unique derivative at a point, so libraries choose a valid convention that supports optimization.</li><li><strong>Finite differences provide an independent check.</strong> Compare a directional derivative with symmetric perturbations across several step sizes; steps that are too large incur approximation error, while tiny steps suffer cancellation.</li></ul><p><strong>The gotcha.</strong> A gradient is local. It does not identify a global optimum, a causal effect, or a safe step size.</p><h2>Backpropagation is a memory and graph computation</h2><p>The forward pass creates intermediate values. The backward pass applies vector-Jacobian products in reverse dependency order. Frameworks retain enough graph state to compute those products.</p><ul><li><strong>Broadcasting reverses through reduction.</strong> A value broadcast across positions receives the sum of gradient contributions from those positions.</li><li><strong>In-place mutation can invalidate saved values.</strong> Autodiff needs the version used during the forward computation.</li><li><strong>Gradient accumulation is additive.</strong> Clear buffers deliberately or microbatches and repeated backward calls accumulate state.</li><li><strong>Checkpointing recomputes intermediates.</strong> It lowers activation memory and spends additional arithmetic.</li></ul><p><strong>The gotcha.</strong> A finite gradient can still be wrong because the loss, mask, reduction, or detached path encodes the wrong computation.</p>`,
  'probability-and-statistics': `<h2>Probability statements require a population and conditioning set</h2><p>A probability is defined relative to a model or sampling process. The useful question is never merely “what is the probability?” It is “for which population, given which information, measured at what time?”</p><ul><li><strong>Base rates shape posterior conclusions.</strong> A highly accurate detector can produce mostly false alarms when the event is rare.</li><li><strong>Conditional independence is an assumption.</strong> Factoring a joint distribution makes inference tractable and can be badly wrong when the omitted dependence carries signal.</li><li><strong>Calibration and ranking differ.</strong> A model can rank examples well while its reported probabilities remain systematically too high or low.</li><li><strong>Selection changes the estimand.</strong> Logged users, labeled cases, and completed outcomes may exclude exactly the cases a deployed policy affects.</li></ul><p><strong>The gotcha.</strong> More samples reduce random error around the quantity sampled; they do not remove selection bias or repair the wrong target.</p><h2>Uncertainty belongs around comparisons</h2><p>A sample statistic varies across possible samples. Standard errors, confidence intervals, posterior intervals, and resampling methods express different uncertainty claims and depend on different assumptions.</p><ul><li><strong>Dependence reduces effective sample size.</strong> Repeated events from one user, adjacent time points, and near-duplicate documents do not provide independent evidence.</li><li><strong>Multiple testing creates discoveries by chance.</strong> Searching many metrics, slices, checkpoints, and prompts requires correction or a protected confirmation set.</li><li><strong>Effect size answers a different question from significance.</strong> A tiny operationally irrelevant change can become statistically clear with enough data.</li><li><strong>Distribution shift changes the target.</strong> An interval computed under yesterday's sampling process does not guarantee coverage after policy or population change.</li></ul><p><strong>The gotcha.</strong> A narrow interval around a biased estimate communicates precision without accuracy.</p>`,
  'optimization': `<h2>The objective defines what improvement means</h2><p>Optimization searches a mathematical objective under constraints. Model behavior follows the combined objective, data, parameterization, and algorithm, including shortcuts the designer did not intend.</p><ul><li><strong>Convexity separates local from global structure.</strong> For a convex objective every local minimum is global; neural-network objectives generally lack this guarantee.</li><li><strong>Conditioning controls directional difficulty.</strong> Narrow curved valleys make one learning rate too small along one direction and unstable along another.</li><li><strong>Regularization changes the fitted solution.</strong> Weight penalties, early stopping, augmentation, and noise encode different preferences and cannot be reduced to one generic “avoid overfitting” switch.</li><li><strong>Constraints need an enforcement mechanism.</strong> Projection, parameterization, penalties, and Lagrange multipliers create different feasibility and numerical tradeoffs.</li></ul><p><strong>The gotcha.</strong> Successfully minimizing a proxy can worsen the actual decision when the proxy omits cost, calibration, robustness, or human impact.</p><h2>Optimizers transform noisy gradient information</h2><p>Stochastic gradients estimate a full-data direction from a batch. Momentum smooths directions across steps; adaptive methods rescale coordinates using gradient history; schedules change the step scale through training.</p><ul><li><strong>Batch size changes noise and systems behavior.</strong> Larger batches improve hardware utilization and gradient stability while spending memory and reducing update frequency.</li><li><strong>Momentum carries state.</strong> Resuming without optimizer buffers changes the trajectory even if weights and learning rate match.</li><li><strong>Adam and AdamW implement different decay semantics.</strong> Decoupled weight decay should not be described as an identical L2 penalty under adaptive scaling.</li><li><strong>Clipping changes extreme updates.</strong> It can prevent numerical failure while hiding the upstream source of exploding gradients.</li></ul><p><strong>The gotcha.</strong> Comparing optimizers without matching tuning budget, schedule, batch, precision, and stopping rule compares experiments rather than algorithms.</p>`,
  'information-theory': `<h2>Information measures depend on a distribution</h2><p>Entropy measures expected uncertainty under one distribution. Cross-entropy measures the expected code length when observations follow one distribution and predictions use another. KL divergence measures their excess coding cost and is asymmetric.</p><ul><li><strong>Zero-probability assignments are catastrophic.</strong> An observed event given probability zero has infinite log loss, which is why numerical implementations clip or use stable logits.</li><li><strong>Conditional entropy measures remaining uncertainty.</strong> Mutual information is the reduction in uncertainty about one variable after observing another.</li><li><strong>Differential entropy is coordinate-dependent.</strong> Continuous entropy can be negative and does not behave like the discrete quantity under rescaling.</li><li><strong>Tokenization changes the unit.</strong> Per-token language-model losses cannot be compared directly across different token vocabularies and segmentation rules.</li></ul><p><strong>The gotcha.</strong> Lower cross-entropy on fixed text measures predictive compression, not truth, reasoning, safety, or task value.</p><h2>Compression connects prediction to representation</h2><p>A predictor that assigns high probability to likely events can encode them with shorter descriptions. This link explains why next-token prediction learns structure, while also marking what the objective leaves unspecified.</p><ul><li><strong>Source coding exploits statistical regularity.</strong> The attainable average code length approaches entropy only under stated source assumptions and sufficiently capable codes.</li><li><strong>Lossy compression defines a distortion measure.</strong> The rate-distortion tradeoff depends on which differences the metric treats as costly.</li><li><strong>Bottlenecks can discard nuisance variation.</strong> Compression helps only when the discarded information is irrelevant to the downstream task.</li><li><strong>Minimum description arguments include the model.</strong> A compact residual paired with an enormous decoder is not a compact explanation.</li></ul><p><strong>The gotcha.</strong> Calling a representation “compressed” is incomplete until the retained task, acceptable distortion, decoder, and total storage cost are stated.</p>`,
};

export function deepenMathematics(chapter) {
  return additions[chapter.id] ? {...chapter, html: chapter.html + additions[chapter.id]} : chapter;
}
